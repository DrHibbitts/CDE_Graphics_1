function [ F ] = evalF( x )
% Calculates 
F = zeros(2,1);
P = evalin('base','P');
for i = 1:length(x)
    theta = (x(i) * pi) / 180;
    F(1) = F(1) + cos(theta);
    F(2) = F(2) + sin(theta);
end
F = F - P;
end

